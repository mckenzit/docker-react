language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t mckenzit/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mckenzit/docker-react npm run test
