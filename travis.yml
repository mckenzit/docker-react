language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t mckenziet/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mckenziet/docker-react npm run test
